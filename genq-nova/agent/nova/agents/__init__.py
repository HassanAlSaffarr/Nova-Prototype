"""Synthetic GENQ intelligence agents: Roberto, Namroud, Peter, Data Chef.

Each generator returns a list of `nova.signals.Signal`. They are deterministic
(seeded) so the demo is reproducible. Some signals are anchored near Nova's CV
detections to demonstrate cross-agent corroboration; the rest are scattered
across Karrada.
"""
